name: baseline_v2
dataset: gpla
shot: 5
use_self_attention: false
use_cross_attention: false
seed: 42
lr: 0.0005
epochs: 100
episodes_per_epoch: 200
embed_dim: 128
data_root: data/processed
conformer_kwargs:
  ffn_dim: 128
  num_heads: 4
  num_layers: 2
